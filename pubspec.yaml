name: baystream
description: Aplicación de gestión de carga marítima - Parser BAPLIE 2.2.1
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  flutter_riverpod: ^3.1.0
  riverpod_annotation: ^4.0.0
  
  # Firebase (comentado temporalmente - causa error CMake en Windows)
  # firebase_core: ^4.4.0
  # cloud_firestore: ^6.1.2
  
  # Utilities
  equatable: ^2.0.5
  dartz: ^0.10.1
  uuid: ^4.3.3
  intl: ^0.20.2
  file_picker: ^10.3.10
  
  # UI
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  riverpod_generator: ^4.0.0+1
  build_runner: ^2.4.8
  mockito: ^5.4.4

flutter:
  uses-material-design: true
